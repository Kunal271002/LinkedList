 // if(head->prev == NULL && head->next->Data >= NewData){
        //     head->prev = NewNode;
        //     NewNode->prev = NULL;
        //     NewNode->next = head;
        //     head = NewNode;
        //     return head;
        // }